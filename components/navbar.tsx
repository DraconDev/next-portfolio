type Props = {};

const Navbar = (props: Props) => {
    return (
        <div className="fixed left-0 top-0 h-8 bg-secondary bg-accent w-full">
            Navbar
        </div>
    );
};

export default Navbar;
