import SectionContainer from "../layout/SectionContainer";

type Props = {};

const Contact = (props: Props) => {
    return (
        <SectionContainer
            name="Contact"
            id="contact"
        >
            <div className=""></div>
        </SectionContainer>
    );
};

export default Contact;
